extends layout 

block content 
    h1= title 

    ul 
        each cat in cat_list
            li 
                a(href=cat.url) #{cat.name}
                |  (#{cat.description})
        else 
            li There are no categories.